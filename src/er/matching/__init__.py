"""Splink 4 settings, training, incremental and full scoring (S4.3).

`model.py` also carries the S4.2 blocking generator: `blocking:` reaches both dbt
and Splink through :func:`er.matching.model.blocking_rules_from_config` and through
nothing else.
"""
